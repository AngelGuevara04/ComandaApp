<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:ComandaApp.PageModels"
             x:Class="ComandaApp.Pages.AdminDashboardPage"
             x:DataType="vm:AdminDashboardPageModel"
             Title="Centro de Mando">

    <ScrollView>
        <VerticalStackLayout Padding="25" Spacing="25">

            <Label Text="Panel de Administración" 
                   FontSize="28" 
                   FontAttributes="Bold" 
                   HorizontalOptions="Center" 
                   TextColor="#512BD4"/>

            <FlexLayout Wrap="Wrap" 
                        JustifyContent="SpaceBetween" 
                        AlignItems="Stretch">

                <Border StrokeShape="RoundRectangle 12" 
                        Padding="15" 
                        Margin="5"
                        FlexLayout.Grow="1"
                        FlexLayout.Basis="250"
                        BackgroundColor="{AppThemeBinding Light=White, Dark=#1E1E1E}">

                    <VerticalStackLayout Spacing="10" HorizontalOptions="Center">
                        <Label Text="Gestión de Dispositivos" 
                               FontAttributes="Bold" 
                               HorizontalOptions="Center" 
                               FontSize="16"/>

                        <Label Text="Mesas, Cajas y Cocinas" 
                               FontSize="12" 
                               TextColor="Gray" 
                               HorizontalOptions="Center"/>

                        <Button Text="Administrar Accesos" 
                                Command="{Binding AbrirDispositivosCommand}" 
                                BackgroundColor="#512BD4"/>
                    </VerticalStackLayout>
                </Border>

                <Border StrokeShape="RoundRectangle 12" 
                        Padding="15" 
                        Margin="5"
                        FlexLayout.Grow="1"
                        FlexLayout.Basis="150"
                        BackgroundColor="{AppThemeBinding Light=White, Dark=#1E1E1E}">

                    <VerticalStackLayout Spacing="10" HorizontalOptions="Center">
                        <Label Text="Menú" 
                               FontAttributes="Bold" 
                               HorizontalOptions="Center" 
                               FontSize="16"/>

                        <Label Text="Platillos y precios" 
                               FontSize="12" 
                               TextColor="Gray" 
                               HorizontalOptions="Center"/>

                        <Button Text="Gestionar" 
                                Command="{Binding AbrirMenuCommand}" 
                                BackgroundColor="#512BD4"/>
                    </VerticalStackLayout>
                </Border>

                <Border StrokeShape="RoundRectangle 12" 
                        Padding="15" 
                        Margin="5"
                        FlexLayout.Grow="1"
                        FlexLayout.Basis="150"
                        BackgroundColor="{AppThemeBinding Light=White, Dark=#1E1E1E}">

                    <VerticalStackLayout Spacing="10" HorizontalOptions="Center">
                        <Label Text="Corte de Caja" 
                               FontAttributes="Bold" 
                               HorizontalOptions="Center" 
                               FontSize="16"/>

                        <Label Text="Finanzas del día" 
                               FontSize="12" 
                               TextColor="Gray" 
                               HorizontalOptions="Center"/>

                        <Button Text="Ver" 
                                Command="{Binding AbrirCorteCommand}" 
                                BackgroundColor="#512BD4"/>
                    </VerticalStackLayout>
                </Border>

                <Border StrokeShape="RoundRectangle 12" 
                        Padding="15" 
                        Margin="5"
                        FlexLayout.Grow="1"
                        FlexLayout.Basis="250"
                        BackgroundColor="{AppThemeBinding Light=White, Dark=#1E1E1E}">

                    <VerticalStackLayout Spacing="10" HorizontalOptions="Center">
                        <Label Text="Reportes e Historial" 
                               FontAttributes="Bold" 
                               HorizontalOptions="Center" 
                               FontSize="16"/>

                        <Label Text="Consulta de pedidos" 
                               FontSize="12" 
                               TextColor="Gray" 
                               HorizontalOptions="Center"/>

                        <Button Text="Consultar" 
                                Command="{Binding AbrirHistorialCommand}" 
                                BackgroundColor="#512BD4"/>
                    </VerticalStackLayout>
                </Border>

            </FlexLayout>

            <Button Text="Configuración del Restaurante" 
                    Command="{Binding AbrirConfigCommand}"
                    BackgroundColor="#6C757D"
                    FontAttributes="Bold"
                    HeightRequest="55"
                    Margin="0,10,0,0" />

            <Button Text="Salir de sesión" 
                    Command="{Binding SalirSesionCommand}"
                    BackgroundColor="#C0392B"
                    TextColor="White"
                    FontAttributes="Bold"
                    HeightRequest="55"
                    CornerRadius="12"
                    Margin="0,5,0,0" />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>